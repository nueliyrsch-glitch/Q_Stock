<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickStock">
        <activity
            android:name=".settings"
            android:exported="false" />
        <activity
            android:name=".supplier"
            android:exported="false" />
        <activity
            android:name=".supplier"
            android:exported="false" /> <!-- Dashboard Activity -->
        <activity
            android:name=".Dashboard"
            android:exported="false" /> <!-- SignIn Activity -->
        <activity
            android:name=".SignIn"
            android:exported="false" /> <!-- Guest Activity -->
        <activity
            android:name=".LogIn"
            android:exported="false" /> <!-- Screen Activity -->
        <activity
            android:name=".Screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Account Activity -->
        <activity
            android:name=".Account"
            android:exported="false" /> <!-- Add Inventory Activity -->
        <activity
            android:name=".Inventory"
            android:exported="false" /> <!-- Add this line to declare Inventory Activity -->
        <activity
            android:name=".ProductDetail"
            android:exported="false" />
    </application>

</manifest>